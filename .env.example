# Copy this file to .env and fill in only the provider(s) you plan to use
# for the frontier judge (see config.yaml's `judge:` section). .env is
# gitignored -- never commit real keys.

ANTHROPIC_API_KEY=
OPENAI_API_KEY=
GOOGLE_API_KEY=
OPENROUTER_API_KEY=
